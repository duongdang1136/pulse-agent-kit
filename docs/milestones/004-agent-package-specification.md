# Milestone 3 / PR #1 — Agent Package Specification

Delivered:

- provider-independent manifest schema v2;
- agent manifest models and YAML loader;
- package file validator;
- migrated Researcher and ITBA manifests;
- unit tests;
- RFC-0001.

Excluded: LLM calls, model/provider configuration, API keys, runtime,
workflow engine, and context packaging.
